# Plan: Implementar funcionalidad de acciones en página de analíticas

## Objetivo
Dar funcionalidad a los iconos de ver (Eye) y borrar (Trash2) en la columna de Acciones de la tabla de reportes en `/app/analiticas/page.tsx`, y eliminar el icono de editar (Pencil).

## Cambios requeridos

### 1. Eliminar icono de editar
- Remover el `<Button>` que contiene el icono `<Pencil />` en la columna de acciones

### 2. Añadir funcionalidad al icono de ver (Eye)
- Añadir `onClick` handler al botón con icono `<Eye />`
- Implementar función `handleViewReport(reportId)` que navegue a la página de detalle del reporte
- Asumir que existe una ruta `/mis-reportes/[id]` para ver detalles individuales

### 3. Añadir funcionalidad al icono de borrar (Trash2)
- Añadir `onClick` handler al botón con icono `<Trash2 />`
- Implementar función `handleDeleteReport(reportId)` que:
  - Muestre diálogo de confirmación (opcional pero recomendado)
  - Llame a `FirestoreService.delete('records', reportId)`
  - En caso de éxito: elimine el reporte del estado y muestre toast de éxito
  - En caso de error: muestre toast de error
  - Maneje estados de carga si es necesario

### 4. Actualizaciones de estado y efectos
- Asegurar que después de eliminar un reporte, la lista se actualice
- Considerar usar el mismo método `loadAllReports()` existente para refrescar la lista

### 5. Componentes y hooks necesarios
- Verificar importaciones existentes: `useRouter` de next/navigation para navegación
- Verificar si se necesita importar componentes de diálogo (si se implementa confirmación)
- Los toasts ya están importados desde "sonner"

## Archivos a modificar
- `Web/app/analiticas/page.tsx` (únicamente este archivo)

## Detalles de implementación

### En el JSX de la tabla:
```jsx
{filteredReports.map((report) => (
  <TableRow key={report.id}>
    {/* ... celdas existentes ... */}
    <TableCell className="text-right">
      <div className="flex items-center justify-end gap-1">
        <Button 
          variant="ghost" 
          size="icon" 
          className="h-8 w-8"
          onClick={() => handleViewReport(report.id)}
        >
          <Eye className="h-4 w-4" />
        </Button>
        <Button 
          variant="ghost" 
          size="icon" 
          className="h-8 w-8 text-destructive hover:text-destructive"
          onClick={() => handleDeleteReport(report.id)}
        >
          <Trash2 className="h-4 w-4" />
        </Button>
      </div>
    </TableCell>
  </TableRow>
))}
```

### Funciones a implementar:
```javascript
const [deletingId, setDeletingId] = useState(null);
// Para mostrar estado de carga en el botón de eliminar

const handleViewReport = (id) => {
  // Navegar a la página de detalle
  // Asumiendo uso de next/navigation
  // router.push(`/mis-reportes/${id}`);
};

const handleDeleteReport = async (id) => {
  // Opcional: mostrar confirmación
  // if (!window.confirm('¿Está seguro de eliminar este reporte?')) return;

  setDeletingId(id);
  try {
    await FirestoreService.delete('records', id);
    // Refrescar la lista
    await loadAllReports(); // Reutilizar función existente
    toast.success('Registro eliminado exitosamente');
  } catch (error) {
    console.error('Error eliminando registro:', error);
    toast.error('Error al eliminar el registro');
  } finally {
    setDeletingId(null);
  }
};
```

## Consideraciones
- Verificar si existe la ruta `/mis-reportes/[id]` y su componente
- Si no existe, podría ser necesario crear un modal en lugar de navegación
- El botón de eliminar debería mostrar estado de carga (spinner o texto)
- Considerar eliminar el estado de carga si no es crítico para la experiencia
- Asegurar que las funciones estén dentro del componente para tener acceso a `setReports`, `reports`, etc.

## Pruebas
- Verificar que el icono de editar ya no aparece
- Verificar que al hacer clic en el ojo, navega correctamente (o abre modal)
- Verificar que al hacer clic en el basurero, elimina el registro y actualiza la lista
- Verificar manejo de errores en eliminación